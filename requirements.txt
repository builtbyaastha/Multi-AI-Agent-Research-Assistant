# Full local development requirements.
#
# Covers: pipeline.py / agents.py / tools.py / memory.py (core pipeline),
# app.py (Streamlit UI), app_server.py + static/ (FastAPI + WebSocket UI),
# mcp_server.py (MCP tool server), eval.py (eval harness),
# tests/test_guards.py (unit tests), plus lint/CI tooling.
#
# For deployment (Render, etc.), use requirements-deploy.txt instead — it's
# a deliberately minimal subset of this file. If you add a new import
# anywhere in the project, add the package here (and to
# requirements-deploy.txt too, if the deployed app needs it).

# --- Core LLM / agent framework ---
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langgraph>=1.2.1

# --- LLM providers ---
langchain-ollama>=0.2.0
langchain-google-genai>=2.0.0

# --- Search & web scraping ---
exa-py>=2.13.0
beautifulsoup4>=4.12.0
requests>=2.31.0

# --- Memory / vector store ---
chromadb>=1.5.9
# Note: no sentence-transformers/torch needed — memory.py uses Chroma's
# built-in ONNX-based embedding function instead, which is both lighter
# and avoids the sentence-transformers/huggingface_hub version conflicts
# hit during local setup.

# --- Structured output / config ---
pydantic>=2.5.0
python-dotenv>=1.0.0

# --- Streamlit UI (app.py) ---
streamlit>=1.38.0

# --- FastAPI UI (app_server.py + static/) ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# --- MCP server (mcp_server.py) ---
mcp>=1.0.0

# --- Dev tooling / CI ---
pytest>=8.0.0
ruff>=0.6.0